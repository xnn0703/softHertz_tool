# AFDTR/QS 双型号上位机开发记录

## 基线

- 分支：feat/afdtr-v2.1-multi-subarray。
- 基线自动化测试：64 passed。

## 记录

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 计划与验收文档 | 已完成 | 本目录三份文档已建立 |
| 双型号主窗口 | 已完成 | `QStackedWidget` 切换 AFDTR/AFD01_QS，`QSettings` 记忆，切换前断开全部串口 |
| QS 协议/页面 | 已完成 | A0 约 2 秒滑动上报频率，500ms 刷新，断流 1 秒显示超时 |
| 报文监视与落盘 | 已完成 | TX/RX/DROP、筛选/复制/另存/暂停，100ms 批刷，10000 行，50MiB 异步轮转 |
| 阵列可视化 | 已完成 | TX/RX 独立 8x8 网格支持 8/7/6/5/4 档，4x4 掩码 `0x3C` |
| 模拟器/测试 | 已通过 | 原 64 项保持，合计 75 passed；含 A0 频率算法与 UI 超时显示 |
| 打包验证 | 已通过 | PyInstaller 名称仍为 `SoftHertz_AFDTR_Tool`，macOS 本地打包并启动到主界面 |

## 连续流与负载记录

- 30.000s 模拟器输出 3000 帧 A0，平均 99.999Hz。
- 帧间隔 min/avg/max = 8.683/10.000/11.136ms，4ms 内连续突发为 0。
- 3000 帧经 Qt 信号、A0 解码、报文监视与异步日志链路，记录 3000 行，无丢帧。
- Windows EXE 实际产物仍需由 GitHub Actions/Windows 环境确认；本地证据不替代 Windows 验收。
