# Windows Python 3.11 发布环境。
# PySide6 6.9.3 是 Windows 10 1809+ 的受控发布基线；升级前必须通过 Windows 原生启动冒烟。
PySide6==6.9.3
PySide6-Addons==6.9.3
PySide6-Essentials==6.9.3
shiboken6==6.9.3
pyserial==3.5

# PyInstaller 与 hooks 必须成对固定，避免 tag 构建随 PyPI 最新版本漂移。
PyInstaller==6.20.0
pyinstaller-hooks-contrib==2026.6
pytest==9.0.3
