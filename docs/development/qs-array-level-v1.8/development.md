# AFD01_QS 阵列档位上位机开发记录

## 2026-07-28

- 用户确认客户界面按档位 `1～5` 展示，对应 `8×8～16×16` 有效子阵。
- 客户界面与协议不暴露芯片布局和内部掩码。
- `protocol.py` 已建立只含 `level/subarray_edge` 的显式 Profile；0x0B 设置只接受
  `1～5/None`。
- 根据最新精简要求，A1 载荷已从五字段改为精确 2 字节
  `tx_level, rx_level`；解码结果也只保留这两个线协议字段。旧 5 字节 A1 明确按长度错误拒绝。
- 0x0B 查询帧和帧长度保持不变；旧值 `6～8` 在构帧、A1 解码和模拟器中明确拒绝，
  不增加不可判定的双编码自动兼容。
- Driver 的 TX/RX 摘要均使用“档位 + 有效子阵规模”，原始十六进制帧仍完整保留。
- Panel 下拉框、确认状态和 Driver 接口已统一使用 level 命名；默认档位为 5。收到 A1 后只按
  TX/RX 当前档位更新下拉框、网格和摘要，不再解释结果、电源或应用状态。
- TX/RX 可视化已改为 16×16、R1～R16/C1～C16 的客户子阵网格，五档按低编号区域
  显示 64/100/144/196/256 个有效单元。
- 模拟器默认档位 5，并复用正式协议 Profile 校验设置值。A1 只回当前两档位；非法设置不改变
  当前档位，但仍返回当前两字节快照。
- QS 定向测试已通过 29 项；1400×650 offscreen 页面截图检查
  未发现控件截断或网格重叠。
- 请求发送失败和 3 秒超时后，当前 TX/RX 网格会从黄色待确认转为红色失败；旧连接的
  延迟 A1 回调受连接代际保护，不会覆盖新连接状态。
- QS 定向测试：`29 passed in 0.83s`。
- 完整 pytest：`143 passed in 4.20s`；compileall 与 Qt offscreen 冒烟通过。
- 30.1 秒内存串口模拟链路收到 3010 帧 A0，平均 `99.999 Hz`，间隔
  `8.969～10.889 ms`；异步日志 3010 行，与接收记录数一致。
- `git diff --check` 通过。
- 当前状态：主机实现与验证完成；Windows EXE 和真实设备验收仍待执行。
