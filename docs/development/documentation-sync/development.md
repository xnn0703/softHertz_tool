# 文档与代码注释同步实施记录

状态：已完成

## 1. 基线

- 基线提交：`dc93908`
- 正式代码范围：`src/soft_hertz_tool`、`packaging`
- 基线正式测试：`125 passed`
- 初始 AST 统计：46 个 Python 模块、36 个类、348 个函数或方法；
- 初始缺口：1 个模块、13 个类、287 个函数或方法缺少 docstring。

## 2. 实施约束

- 只修正文档和注释，不改变业务行为；
- 当前实现与长期文档冲突时，以源码、测试和构建配置为准；
- Windows 原生、真实设备和缺失协议原件继续保持开放；
- README 继续作为纯项目文档，不记录协作过程。

## 3. 结果

### 3.1 文档同步

- 将 AFD01_QS 能力准确收敛为 `0x01`～`0x0A` 控制、`0x0B/0xA1` 阵列和 `0xA0` 状态；
- 补充 `run.sh`、`run.bat` 的模拟器模式及 `app --smoke` 边界；
- 明确当前 `0.0.0` 是开发占位版本，tag、Release 和包元数据尚未统一；
- 将可观测性扩展契约统一为 `FrameRecord.port`，不引入平行 `endpoint` 字段；
- 补全资源、产品身份、入口、打包和 docstring 契约测试文件；
- 移除长期文档中容易漂移的历史测试数量描述；
- 保持 Windows 原生、真实设备、QS 受控协议原件和日志保留策略为开放门槛。

### 3.2 注释覆盖

- 最终范围：46 个 Python 模块、36 个类、348 个函数或方法；
- 模块、类、函数和方法 docstring 缺失均为 0；
- 新增 AST 契约测试，后续遗漏会报告相对路径、行号、节点类型和名称；
- 在工作区切换、线程停止、连接代际、流恢复、查询调度、阵列超时、日志轮转、
  高频 UI 限频、模拟器调度和打包清理处补充设计原因说明。

### 3.3 行为保持验证

对 35 个发生注释变更的生产 Python 文件，分别解析基线版本和当前版本，移除模块、类和函数
docstring 后比较 AST；结果为 0 个差异。该检查表明本次没有改变协议、UI、串口、线程、日志或
打包控制流。

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| docstring AST 契约 | 通过，缺失 0 |
| 去除 docstring 后的生产代码 AST 对比 | 35 个文件，差异 0 |
| `QT_QPA_PLATFORM=offscreen python -m pytest -q` | `126 passed` |
| `python -m compileall -q src tests packaging` | 通过 |
| Python 3.9 AST 语法解析 | 46 个生产/打包文件通过 |
| `python -m soft_hertz_tool --smoke` | 通过 |
| 两个模拟器模块 `--help` | 通过 |
| `python -m pip check` | 通过 |
| Markdown 本地链接检查 | 16 个文件，缺失 0 |
| `git diff --check` | 通过 |

## 5. 未改变的验收边界

- 当前 Windows EXE 尚未完成干净 Windows 客户环境原生验收；
- KaUDC004A、AFDT1024、AFDR1024 和 AFD01_QS 真实设备闭环仍待完成；
- AFD01_QS V1.6 受控协议原件仍待补齐；
- 日志总容量、保留时间、归档和自动清理策略仍待定义。
