# 验收标准

状态：2026-09-09 软件验收通过，实板与 Windows EXE 验收待执行。

- 全量离屏 pytest：233 passed（含 TA/RA）；首批内部指令定向：75 passed。
- compileall 和 git diff --check 通过；客户控制和内部测试两个标签页完成 1450×1000 离屏截图检查。
- 新内部 codec、流拆包、模拟器闭环、真实 Panel → Driver 发送与快照展示均有回归覆盖。
- 新内部 DOCX 黄金帧由正式 codec 生成，客户原件未修改。

冻结验收标准：

ta/ra 增补软件通过：覆盖 0x46/47 构帧解析、BF0/BF1 范围、未选中侧、真实 UI 发送边界及模拟器查询；
检查旧客户衰减和 0x45 未受影响。软件验收完成后另做实板 RF 衰减验收。

- 正常/异常构帧与解析、请求响应映射、拆包粘包、golden CRC 向量。
- 模拟器独立开关、客户覆盖、行列和角度请求、内部状态查询闭环。
- UI 通过 Driver 发送、断连禁止操作、错误可见；请求和发送快照分开展示。
- KA_RF_UNIT 专项 pytest、依赖边界、必要集成测试和 compileall；界面可视检查。
- 内部协议说明与固件合同、新 DOCX 完全一致。
- 原有客户控制与扫描功能回归；Windows 原生与真实设备验收单独记录，不由模拟器代替。
# 紧凑布局增补验收（2026-09-09）

- PASS：新增实际 Qt 布局回归先复现客户页 315 px 内容被撑到 564 px，再验证修复。
- PASS：客户/内部/客户切换按当前页取高，角度与衰减输入 100–110 px，TX/RX 行列水平对齐。
- PASS：1450×950 离屏截图检查两页，客户命令区 350 px、内部 390 px，三列状态表紧接命令区。
- PASS：全量 pytest 234 passed；compileall 与 git diff --check 通过。
- 边界：未连接硬件；Windows 原生样式和不同系统缩放需现场确认，小屏允许滚动。
