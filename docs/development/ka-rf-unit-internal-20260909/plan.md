# KA RF UNIT 内部测试上位机计划

## 布局增补（2026-09-09 用户已确认）

命令区按当前标签页内容取高度；内部 PA/IF 限宽，TX/RX 行列并排，角度与 TA/RA 双列分组。
保留全部控制及状态语义，不改协议。验收：切换页面无隐藏页占高、数值输入不拉伸、行列对齐、
两页截图检查及全量上位机回归；小屏仍允许滚动，不以裁切状态换取一屏。

状态：用户已确认；2026-09-09 软件实现及回归完成，实板与 Windows 原生验收待执行。

协议设计与硬件语义以配套固件工程
`code/target/ka_rf_unit/doc/internal_test_20260909/plan.md` 为本次联合计划。
新增 0x40 PA、0x41 TX IF、0x42 RX IF、0x43 行列掩码、0x44 角度波束、0x45 内部状态查询。
客户指令不改编号或帧布局。内部设置不持久化；内部与客户控制的覆盖关系依照联合计划。

修改范围：KA_RF_UNIT protocol/stream/driver/panel/simulator、对应测试、协议说明与 README。
Panel 提供明确标注的内部测试区、独立开关、TX/RX 各 8 行与 8 列、角度与状态；
Driver 提供语义接口；Simulator 导入正式 codec；共享串口与报文监视机制保持现有架构。
上位机直接发送角度请求，由主控按当前配置 RF 换算，复用现有离轴/方位角定义。
界面区分请求值、协议应答与主控命令发送状态，不把它们表述为物理 RF 应用确认。

开发证据见 development.md，验收记录见 acceptance.md；当前线协议见协议 readable-notes。

## ta/ra 增补（用户已确认，软件实现完成）

增加阵列干路/支路衰减，不复用变频衰减控件。联合固件计划新增 0x46 设置、0x47 查询，
原 0x45 保持不变；BF0 干路 0/8 dB，BF1 干路及两型支路 0..7.5 dB、步进 0.5。
内部页展示 TX/RX BF 类型、发送记录有效性；同步 driver/protocol/simulator、测试及内部 DOCX。
