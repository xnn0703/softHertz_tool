# 开发记录

2026-09-09：已读取工程 AGENTS.md 和开发规范，完成联合计划与验收标准。
当前只新增计划文档，生产代码、测试上位机界面和内部 DOCX 尚未实现，待范围确认。

## 用户确认后的实现

- protocol/driver/panel/simulator 同步 0x40..0x45，保持原有客户命令布局。
- 使用“客户控制 / 内部功能测试”两个标签页。内部页提供独立 PA/TX IF/RX IF、行列位、角度和查询。
- 内部区随连接启用，断连清理快照；连接代际拦截旧信号；查询结果不覆盖操作输入。
- 角度直接发 0x44，由固件按最近接受 RF 转换；显示频点来源；未选中阵面不校验、不计算。
- 模拟器保留 IF 独立状态，客户阵列命令覆盖 mask/PA 而不覆盖 IF；0xC5 按 13 字节解释。
- 回归揭示 PSA 帧头跨分包时 P/PS 被丢弃，stream 保留帧头有效后缀；遍历全部拆分位置验证。
- 全量测试 218 passed，定向 75 passed；compileall、diff 检查和两页离屏 UI 检查通过。
- 尚未连接真实串口和 RF 设备；模拟器成功不等于物理应用确认；未打包 Windows EXE。
- 内部 Word 协议共 8 页，按 documents 渲染流程逐页检查中文和表格布局通过，另存内部文件名。

## TA/RA 和文档格式增补（用户已确认）

- 完成 0x46 阵列干路/支路衰减和 0x47 查询，客户 0x11 与内部 C5 布局保持不变。
- protocol/driver/panel/simulator 同步实现；模拟器 BF 配置仅为模拟值，真实 BF 由固件 INFO 决定。
- TA、RA 各一行，独立设置；查询不回填输入，断连清理，旧连接信号由代际门禁拒绝。
- 全量 pytest 233 passed；含 BF 范围、选中侧、负值/非半步/非有限数拒绝、黄金帧、拆分及 UI 边界。
- 1450×1000 两标签页离屏检查、compileall、diff 检查通过。实板与 Windows 原生未验收。
- 内部 Word 改为客户原件模板，最终 26 页逐页检查，替代上述早期 8 页简版；字体、编号、
  封面修订表、彩色帧表及原图保留，客户原件未修改。联合固件 development.md 记录模板审计证据。
# 2026-09-09 紧凑布局增补

用户确认后调整页面布局。根因是 QTabWidget 默认以所有页最大高度计算尺寸；
局部命令标签类改用当前页高度、保留 Qt 标签栏尺寸，切换通知父布局重新计算。
内部 PA/IF 同行限宽，TX/RX 行列左右排列，角度与 TA/RA 双列分组；全部信号与输入顺序保留。
未修改 protocol、driver、simulator 或固件。新增回归修复前失败、修复后通过；全套 234 passed。
离屏截图已检查，原生 Windows/缩放验收独立保留。

## Windows 发布布局修正

v3.1.6 CI 的布局回归发现 Windows 角度输入 sizeHint 为 120 px，超过原固定 110 px。
角度与衰减输入在设置范围和单位后采用 max(原紧凑宽度, sizeHint)；保留防裁切回归。
后续发布使用 v3.1.7，v3.1.6 未生成 EXE。
