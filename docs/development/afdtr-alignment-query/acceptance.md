# 校准结果查询验收标准

状态：RX 软件验收通过；TX 延后；2026-09-10。

- [x] RX ID=1 查询与源码对应，完整字节为 `50 53 41 01 01 9E 84`；+0x80 地址正确。
- [ ] TX 延后：用户已确认等待 TX 源码，不在本次范围。
- [x] 正常 17 字节回复可解析全部十项数据；温度原始值 79/80/81 分别显示 -1/0/1。
- [x] 错误长度、校验不会发布有效校准状态；正常回复拆包、粘包可接收。
- [x] 普通“查询全部状态”对每个目标追加一次校准查询，维持原有两项查询和发送间隔。
- [x] UI 按目标 ID 合并，+0x80 回复正确归行；校准结果不覆盖其它状态，LINK_ID 单独保存。
- [x] 未回复设备显示 N/A，其它状态仍更新；重建设备行不会显示其它 ID 的旧校准值。
- [x] 模拟器完成校准查询回复；客户流量模拟两项查询及发送模式回归不变。
- [x] `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest -q` 通过，记录实际数量与环境。
- [x] `.venv/bin/python -m compileall -q src tests packaging` 通过。
- [ ] 真实 AFDR1024 校准值与设备配置对照；真实 AFDT1024 单独验证，未执行明确标注。
- [ ] Windows 原生 EXE 界面与真实串口单独记录，未执行明确标注。

结果：macOS offscreen 全量 pytest 为 344 passed in 12.66s，本次新增13项；compileall通过。状态区展开截图检查通过（模拟值）；真实设备与 Windows EXE 未执行。
