# 校准结果查询开发记录

## 2026-09-10 协议核对与计划

已读取现场 RX 的 UART2_ReturnAlignData：10 字节校准数据，LEN=11，总长 17，温度偏移编码加 80。请求使用无载荷的 7 字节形式。TX 协议尚待确认。

已检查普通 query_status、状态表合并入口和项目开发规范。计划普通状态查询追加校准查询，另设可折叠结果表；避免修改客户流量模拟复用的双查询接口。

当前仅新增计划、验收与本记录，尚未修改业务源码或运行本功能测试。等待范围与 TX 协议确认。保留工作区已有客户流量模拟与 KA_RF_UNIT 并行改动。

## RX 实现与验收

用户已确认“先完成 RX，TX 待源码确认”。实现独立 0x9E 构帧和严格10字节解析、Driver 状态合并、RX 可折叠校准表、模拟器默认校准回复。外层地址低7位用于归行，载荷 LINK_ID 独立保存；温度偏移减80，其余字段保留原始码。普通查询追加第三项，客户流量双查询接口未变。

新增13项测试覆盖固定报文字节、温度边界、异常载荷/校验拒绝、拆粘包和状态合并、真实按钮到Driver查询序列、多ID/+0x80、模拟器RX/TX隔离和重新建表N/A。首次测试发现手写测试向量校验填为CA，独立求和结果为C5，修正测试向量后全量344项通过（12.66s）。compileall通过。offscreen展开状态表截图人工检查通过，模拟数据无标签遮挡，窄面板可横向滚动。

对照plan与acceptance：本次RX软件范围完成；TX延期，Windows原生EXE、真实阵列校准字段回读未执行。没有修改阵列源码或提交Git；保留其它并行改动。
